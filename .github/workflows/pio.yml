name: PlatformIO CI
on: [push]
jobs:
  build:
    runs-on: self-hosted
    steps:
      # self-hosted runner
      - uses: actions/checkout@v3
      # cache
      - name: Cache PlatformIO dependencies
        uses: actions/cache@v3
        with:
          path: |
            ~/.platformio
            ~/.cargo
            target
            .pio
          key: ${{ hashFiles('platformio.ini') }}-${{ hashFiles('**/Cargo.lock') }}

      - name: Setup rust nightly
        uses: actions-rs/toolchain@v1
        with:
          components: rust-src,rust-std
          toolchain: nightly
          override: true

      - name: Remove rust-toolchain.toml
        run: cd workspace && rm rust-toolchain.toml

      - name: Install cargo-pio and espup
        run: |
          cd workspace
          cargo +nightly install cargo-binstall
          cargo +nightly binstall cargo-pio espup -y

      - name: Install PlatformIO
        run: cd workspace && cargo +nightly pio installpio

      - name: Build PlatformIO check
        run: cd workspace && cargo +nightly pio exec -- check

      - name: Build using PlatformIO
        run: |
          cd workspace
          source .env
          espup install -t $MCU -f .espup.sh
          source .espup.sh
          cargo +esp pio build
